{
  "name": "opengenepool-example",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "deploy": "wrangler pages deploy dist",
    "deploy:prod": "wrangler pages deploy dist --branch main"
  },
  "dependencies": {
    "@heroicons/vue": "^2.2.0",
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "@vitejs/plugin-vue": "^5.0.0",
    "wrangler": "^3.0.0"
  }
}
