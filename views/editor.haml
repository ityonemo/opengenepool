%aside#toolbar
  #infobox
    #innerbox
    -if(session[:user])
      %button#forkbutton(type="button")
        fork
  #information
  #graphics.toolitem
    %details
      %summary graphics
      zoom
      %select#zoomer
      %br
      %button#getsvg(type="button" onclick="graphics.getsvg()")
        get svg
  -if(session[:user])
    #directory.toolitem
      %details(open)
        %summary root directory
        %button#addtoroot(type="button" onclick="files.addtoroot()")
          add here
    #workspace.toolitem
      %details(open)
        %summary workspace
        #workspace_content
#editor
#contextmenu
